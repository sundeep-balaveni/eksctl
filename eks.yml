apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: Roboshop
  region: us-east-1

managedNodeGroups:
  - name: spot-ng
    instanceTypes:
      - t3.micro
    spot: false
    minSize: 1
    maxSize: 2
    desiredCapacity: 1