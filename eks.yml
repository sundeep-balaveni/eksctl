apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: Roboshop-v3
  region: us-east-1

managedNodeGroups:
  - name: spot-ng-01
    instanceTypes:
      - t3.micro
    spot: true
    minSize: 1
    maxSize: 2
    desiredCapacity: 1