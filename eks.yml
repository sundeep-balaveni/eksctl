apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: Roboshop
  region: us-east-1

managedNodeGroups:
  - name: spot-ng
    instanceTypes:
      - t3.medium
      - t3.large
      - m5.large
      - c5.large
    spot: true
    minSize: 1
    maxSize: 3
    desiredCapacity: 2